angular.module('intrApp').
controller('testCtrl', ['$scope','Mensajes', function($scope, Mensajes) {
    // ============== Declaraciones ============== //
    $scope.mensajes = [];
    $scope.mensaje = {
      msg : '',
    };
    // ============== INIT ============== //

    // ============ FUNCIONES ============= //

  }
]);
